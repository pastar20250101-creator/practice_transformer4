# 오늘 프로젝트로 배워야 할 것

이 문서는 오늘 진행한 Transformer 실습 프로젝트를 그냥 "제출"로 끝내지 않고, AI 공부의 흐름으로 이해하기 위한 학습 노트입니다.

현재 GitHub 제출 상태는 실습 1, 실습 6, 실습 9입니다.

이 학습 가이드는 제출된 실습뿐 아니라, AI 흐름을 이해하는 데 중요한 실습 2 내용도 함께 포함합니다.

## 오늘의 핵심 한 줄

AI 프로젝트는 크게 네 단계로 볼 수 있습니다.

```text
데이터 준비 -> 모델에 넣을 표현 만들기 -> 결과 생성/검색 -> 평가하고 안전하게 제출하기
```

오늘 한 실습들은 이 흐름을 각각 다른 방식으로 보여줍니다.

## 1. 실습 1에서 배울 것: 텍스트 분석의 기본기

실습 1은 한국어 영화 리뷰를 분석했습니다.

여기서 중요한 건 "AI가 글을 바로 이해하는 것이 아니다"라는 점입니다. 먼저 텍스트를 정리하고, 의미 있는 단어를 뽑고, 빈도를 계산해서 사람이 볼 수 있는 형태로 만들어야 합니다.

배워야 할 개념:

- 텍스트 전처리: 불필요한 문자 제거, 공백 정리, 한글만 남기기
- 토큰화: 긴 문장을 작은 단위의 단어로 나누기
- 품사 필터링: 명사, 형용사처럼 분석에 쓸 단어만 남기기
- 불용어 제거: "영화", "정말"처럼 너무 흔하거나 의미가 약한 단어 제거
- 빈도 분석: 많이 나온 단어를 기준으로 데이터의 분위기 파악
- 시각화: bar plot, word cloud로 결과를 빠르게 이해하기

왜 도움이 되나:

텍스트 AI, 챗봇, 검색, 감성 분석을 배우려면 먼저 텍스트를 모델에 넣기 좋은 형태로 다루는 감각이 필요합니다. 실습 1은 그 입구입니다.

스스로 설명할 수 있어야 하는 질문:

- 왜 원본 텍스트를 그대로 쓰지 않고 전처리를 할까?
- 토큰화가 잘못되면 분석 결과가 어떻게 망가질까?
- 불용어를 너무 많이 지우면 어떤 문제가 생길까?
- word cloud는 예쁘지만 왜 정량적 평가로는 부족할까?

## 2. 실습 2에서 배울 것: Seq2Seq 번역과 Attention

실습 2는 한국어 문장을 영어 문장으로 바꾸는 Seq2Seq 번역 모델을 다룹니다.

여기서 중요한 건 "입력 하나를 보고 출력 하나를 고르는 것"이 아니라, 입력 문장을 읽고 출력 문장을 순서대로 생성한다는 점입니다. 번역, 요약, 챗봇, 이미지 캡셔닝 같은 생성 AI는 대부분 이 구조를 이해하면 훨씬 잘 보입니다.

배워야 할 개념:

- Seq2Seq: 입력 시퀀스를 출력 시퀀스로 바꾸는 모델 구조
- Encoder: 입력 문장을 읽고 의미 정보를 hidden state로 압축하는 부분
- Decoder: 압축된 정보를 바탕으로 출력 단어를 하나씩 생성하는 부분
- Attention: 출력 단어를 만들 때 입력 문장의 어느 부분을 볼지 정하는 방법
- Luong Attention: decoder hidden state와 encoder output을 비교해 attention score를 계산하는 방식
- Teacher Forcing: 학습 중에 모델 예측 대신 정답 단어를 다음 입력으로 넣어 학습을 안정화하는 기법
- Greedy Decoding: 매 순간 가장 확률이 높은 단어를 고르는 생성 방식
- Beam Search: 여러 후보 문장을 동시에 유지하면서 더 좋은 번역을 찾는 방식

왜 도움이 되나:

Seq2Seq는 Transformer 이전 세대의 대표적인 생성 모델 구조입니다. 이걸 이해하면 Transformer의 attention이 왜 중요한지, BLIP이 이미지를 보고 문장을 생성하는 흐름이 왜 encoder-decoder와 닮았는지 연결해서 볼 수 있습니다.

스스로 설명할 수 있어야 하는 질문:

- Encoder와 Decoder는 각각 무슨 일을 할까?
- Attention이 없으면 긴 문장 번역에서 어떤 문제가 생길까?
- Teacher Forcing은 왜 학습을 안정화할까?
- Greedy Decoding과 Beam Search는 어떤 차이가 있을까?
- Seq2Seq와 Transformer는 어떤 문제의식으로 연결될까?

## 3. 실습 6에서 배울 것: CLIP과 멀티모달 검색

실습 6은 CLIP을 이용해서 패션 이미지를 검색했습니다.

CLIP의 핵심은 이미지와 텍스트를 같은 벡터 공간에 올려놓는 것입니다. 예를 들어 "white sneakers"라는 문장과 흰색 운동화 사진이 비슷한 방향의 벡터가 되도록 학습된 모델입니다.

배워야 할 개념:

- 임베딩: 이미지나 문장을 숫자 벡터로 바꾸는 것
- 벡터 공간: 비슷한 의미끼리 가까이 놓이는 숫자 공간
- 코사인 유사도: 두 벡터의 방향이 얼마나 비슷한지 계산하는 방법
- 텍스트-이미지 검색: 문장으로 이미지를 찾는 방식
- 이미지-이미지 검색: 이미지 하나로 비슷한 이미지를 찾는 방식
- 멀티모달 AI: 텍스트, 이미지처럼 서로 다른 데이터 종류를 함께 다루는 AI

왜 도움이 되나:

요즘 AI 서비스는 텍스트만 다루지 않습니다. 이미지 검색, 상품 추천, 스타일 검색, 이미지 설명, 영상 이해 같은 기능은 멀티모달 개념에서 출발합니다. CLIP은 그 개념을 가장 직관적으로 보여주는 모델입니다.

스스로 설명할 수 있어야 하는 질문:

- CLIP은 왜 이미지와 텍스트를 같은 공간에 놓을까?
- "비슷하다"는 것을 숫자로 어떻게 계산할까?
- 코사인 유사도는 왜 크기보다 방향을 볼까?
- 이미지 검색에서 벡터 DB가 왜 필요해질까?

## 4. 실습 9에서 배울 것: 이미지 캡셔닝과 생성 AI

실습 9는 BLIP을 이용해서 이미지 설명 문장을 만들고, 그 결과를 한국어로 번역했습니다.

실습 6이 "찾기"에 가깝다면, 실습 9는 "생성하기"에 가깝습니다. 이미지를 보고 모델이 문장을 만들어내기 때문입니다.

실습 2의 Seq2Seq를 먼저 이해하면 실습 9도 더 잘 보입니다. 실습 2에서는 문장을 읽고 다른 문장을 만들고, 실습 9에서는 이미지를 읽고 설명 문장을 만듭니다.

배워야 할 개념:

- 이미지 캡셔닝: 이미지를 입력으로 받아 설명 문장을 생성하는 작업
- BLIP: 이미지 이해와 언어 생성을 함께 다루는 모델 계열
- 조건부 생성: 이미지나 프롬프트 같은 조건을 주고 문장을 생성하는 방식
- 디코딩: 모델이 다음 단어를 하나씩 선택하며 문장을 만드는 과정
- BLEU score: 생성 문장과 정답 문장이 얼마나 겹치는지 보는 평가 지표
- 번역 모델: 영어 캡션을 한국어 문장으로 변환하는 모델

왜 도움이 되나:

이미지 캡셔닝은 생성형 AI를 이해하는 좋은 예제입니다. 모델이 이미지를 "분류"하는 데서 끝나지 않고, 사람이 읽을 수 있는 문장으로 바꾸는 과정을 볼 수 있습니다.

스스로 설명할 수 있어야 하는 질문:

- 이미지 분류와 이미지 캡셔닝은 무엇이 다를까?
- 생성 모델은 왜 매번 결과가 조금씩 달라질 수 있을까?
- BLEU 점수가 높다고 항상 좋은 캡션일까?
- 영어 캡션을 한국어로 번역할 때 의미가 왜 변할 수 있을까?

## 4. GitHub 제출 과정에서 배울 것

오늘은 모델 코드뿐 아니라 제출 과정도 중요했습니다.

AI 프로젝트는 코드만 잘 짜면 끝이 아닙니다. 데이터 파일 크기, API 키 보안, GitHub 히스토리, 실행 결과 관리도 실무에서 매우 중요합니다.

배워야 할 개념:

- Git commit: 작업 상태를 기록하는 단위
- GitHub push: 로컬 커밋을 원격 저장소에 올리는 것
- `.gitignore`: 올리면 안 되는 파일을 제외하는 규칙
- `.env`: API 키 같은 비밀값을 코드 밖에 두는 파일
- `.env.example`: 어떤 환경변수가 필요한지만 알려주는 예시 파일
- Git LFS: 100MB가 넘는 큰 파일을 GitHub에 올릴 때 쓰는 방식
- secret scanning: GitHub가 키처럼 보이는 문자열을 감지하는 기능
- history cleanup: 이미 올라간 키가 과거 커밋에 남지 않게 히스토리를 정리하는 작업

오늘 특히 중요했던 점:

- API 키를 노트북에 직접 쓰면 안 됩니다.
- 이미 public GitHub에 올라간 키는 노출된 것으로 보고 폐기해야 합니다.
- 큰 JSON 파일은 일반 Git이 아니라 Git LFS로 관리해야 합니다.
- 노트북 출력 이미지의 base64도 키처럼 오탐될 수 있어 제출 전 정리할 수 있습니다.

## 5. 오늘 작업을 AI 공부 순서로 다시 보면

오늘의 실습은 다음 순서로 이해하면 좋습니다.

```text
실습 1: 텍스트를 정리하고 분석한다
실습 2: 문장을 읽고 다른 문장을 생성한다
실습 6: 이미지와 텍스트를 벡터로 바꿔 검색한다
실습 9: 이미지를 보고 문장을 생성한다
GitHub: 결과물을 안전하게 제출한다
```

이 순서는 AI 학습 흐름과도 비슷합니다.

```text
데이터 이해 -> 시퀀스 생성 -> 표현 학습 -> 검색/생성 -> 평가/배포
```

## 6. 내일 복습하면 좋은 순서

### 30분 복습

1. 실습 1 노트북에서 `load_corpus`, `tokenize_and_count` 함수 다시 보기
2. 실습 2 노트북에서 `Encoder`, `LuongAttention`, `Decoder` 클래스 다시 보기
3. 실습 6 노트북에서 `encode_text`, `encode_image`, cosine similarity 부분 다시 보기
4. 실습 9 노트북에서 `processor`, `model.generate`, `decode` 흐름 다시 보기

### 1시간 복습

1. 실습 1 결과 이미지 2개를 보고 어떤 단어가 왜 많이 나왔는지 설명하기
2. 실습 2에서 Encoder -> Attention -> Decoder 흐름을 말로 설명하기
3. 실습 6에서 검색어를 3개 바꿔보며 결과가 왜 달라지는지 생각하기
4. 실습 9에서 캡션 결과와 실제 이미지를 비교하며 틀린 설명 찾기

### 2시간 복습

1. 실습 2에서 Teacher Forcing, Greedy Decoding, Beam Search 차이 정리하기
2. 실습 6에서 텍스트 검색과 이미지 검색의 공통점을 그림으로 정리하기
3. 실습 9에서 BLEU 점수가 높은 문장과 사람이 보기에 좋은 문장이 항상 같은지 비교하기
4. `.env`, `.gitignore`, Git LFS를 직접 설명할 수 있게 정리하기

## 7. 꼭 기억할 단어장

| 용어 | 쉬운 뜻 |
| --- | --- |
| 전처리 | 모델에 넣기 전에 데이터를 정리하는 것 |
| 토큰화 | 문장을 작은 단위로 나누는 것 |
| 임베딩 | 데이터의 의미를 숫자 벡터로 바꾸는 것 |
| 벡터 | 여러 숫자로 이루어진 위치/방향 정보 |
| 코사인 유사도 | 두 벡터가 얼마나 같은 방향인지 보는 값 |
| Seq2Seq | 입력 순서를 출력 순서로 바꾸는 모델 구조 |
| Encoder | 입력 데이터를 읽고 의미 정보를 압축하는 부분 |
| Decoder | 압축된 정보를 이용해 출력을 하나씩 만드는 부분 |
| Attention | 출력할 때 입력의 중요한 부분에 집중하는 방법 |
| Teacher Forcing | 학습 중 정답 단어를 다음 입력으로 넣는 기법 |
| Greedy Decoding | 매 순간 가장 확률 높은 단어를 고르는 생성 방식 |
| Beam Search | 여러 후보 문장을 유지하며 더 좋은 생성을 찾는 방식 |
| 멀티모달 | 텍스트, 이미지 등 여러 종류의 데이터를 함께 다루는 것 |
| 캡셔닝 | 이미지를 설명하는 문장을 만드는 것 |
| 디코딩 | 모델의 숫자 출력을 사람이 읽는 문장으로 바꾸는 것 |
| BLEU | 생성 문장이 정답 문장과 얼마나 겹치는지 보는 지표 |
| Git LFS | 큰 파일을 GitHub에 올리기 위한 도구 |
| `.env` | API 키 같은 비밀값을 따로 저장하는 파일 |

## 8. 스스로 답해볼 질문

아래 질문에 말로 답할 수 있으면 오늘 공부가 꽤 잘 된 것입니다.

1. 한국어 텍스트 분석에서 왜 형태소 분석기가 필요할까?
2. Seq2Seq에서 Encoder와 Decoder는 왜 역할을 나눌까?
3. Attention은 번역에서 어떤 문제를 해결할까?
4. Teacher Forcing은 왜 학습할 때만 쓰고 추론할 때는 못 쓸까?
5. CLIP은 왜 이미지와 문장을 같은 벡터 공간에 놓을까?
6. 이미지 검색에서 cosine similarity가 하는 일은 무엇일까?
7. BLIP은 이미지 분류 모델과 무엇이 다를까?
8. BLEU score만으로 좋은 캡션인지 판단하면 왜 위험할까?
9. API 키를 노트북에 직접 쓰면 왜 위험할까?
10. 100MB 넘는 파일은 왜 Git LFS로 올려야 할까?

## 9. 다음에 배우면 좋은 것

다음 단계는 실습 2를 실제로 실행하면서 번역 모델의 학습 과정을 더 깊게 보는 것입니다.

실습 2는 Seq2Seq 번역 모델입니다. 이걸 직접 돌려보면 "입력 문장을 읽고 출력 문장을 생성한다"는 구조를 몸으로 이해할 수 있습니다. 이후 Transformer의 attention을 배우면, CLIP이나 BLIP 같은 모델이 왜 강력한지 더 잘 보입니다.

추천 순서:

```text
Seq2Seq -> Attention -> Transformer -> CLIP -> BLIP -> RAG/Agent
```

오늘 기준으로는 다음 목표가 좋습니다.

- 실습 2를 실행해서 번역 모델의 encoder/decoder 구조 이해하기
- attention이 왜 필요한지 그림으로 설명하기
- CLIP의 embedding과 Transformer의 hidden state가 어떤 관계인지 연결해보기

## 10. 오늘 프로젝트의 진짜 성과

오늘 한 일은 단순히 노트북 몇 개를 GitHub에 올린 것이 아닙니다.

실제로는 다음 능력을 연습했습니다.

- 데이터를 정리하는 능력
- 시퀀스 생성 모델의 구조를 읽는 능력
- Attention이 왜 필요한지 이해하는 능력
- 모델 입력과 출력 흐름을 읽는 능력
- 벡터 기반 검색을 이해하는 능력
- 생성 모델 결과를 평가하는 능력
- 큰 파일과 API 키를 안전하게 제출하는 능력
- GitHub 저장소를 제출용으로 관리하는 능력

이 능력들이 쌓이면 나중에 직접 AI 서비스를 만들 때 큰 도움이 됩니다.
